import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#394867",
    },
    secondary: {
      main: "#9384D1",
    },
  },
});

export default theme;
